if __name__ == "__main__":
    import subprocess
    tests = open("~/.bash_history").readlines();
    for cmd in tests:
        bashp = subprocess.run(["bash",
        minishellp = subprocess.run("
    
